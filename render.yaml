services:
  - type: web
    name: ebook-factory
    runtime: node
    plan: free
    region: ohio
    branch: production-hardening
    buildCommand: npm ci && npm run typecheck && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NODE_ENV
        value: production
      - key: PUBLIC_BASE_URL
        value: https://ebookfactory.org
      - key: APP_ORIGIN
        value: https://ebookfactory.org
      - key: TRUST_PROXY
        value: "true"
      - key: PAYPAL_ENVIRONMENT
        value: sandbox
      - key: PAYPAL_CLIENT_ID
        sync: false
      - key: PAYPAL_CLIENT_SECRET
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: DATABASE_SSL
        value: "true"
      - key: SESSION_SECRET
        sync: false
